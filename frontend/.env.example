# API Configuration
# Backend API URL (default: http://localhost:3001)
# In production, set this to your backend server URL
# Note: Vite requires VITE_ prefix for environment variables to be exposed to the client
VITE_API_URL="http://localhost:3001"

# Environment
# Set to 'production' for production builds, 'development' for development
NODE_ENV="development"
